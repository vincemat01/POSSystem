# Supabase project (Settings -> API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Used by trusted server routes (e.g. AI endpoints, admin scripts). Never expose to the client.
SUPABASE_SERVICE_ROLE_KEY=

# AI provider (abstracted behind src/lib/ai — see that module before adding a second provider)
ANTHROPIC_API_KEY=

# WhatsApp Cloud API (Phase 8 — not wired up yet)
WHATSAPP_CLOUD_API_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
